/**
 * 데이터 모델 Export
 */

const Consultation = require('./Consultation')
const { MatchHistory, WeekDescriptor } = require('./MatchHistory')

module.exports = {
  Consultation,
  MatchHistory,
  WeekDescriptor,
}

